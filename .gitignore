# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.netlify
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sources de référence G8/GAP (28 Mo de binaires) — synthétisées dans les 3 .md de /docs.
# Conservées localement, hors repo. Retirer cette ligne pour les versionner.
docs/reference-g8/
# Captures d'écran G8 de référence (dossier fourni par le client) — hors repo.
infos app/
# Manuels G8 supplémentaires (gros PDF de référence) — hors repo, synthétisés dans docs/g8-fonctions-*.md
Info Doc/
# DONNÉES CLIENT (factures PDF importées) — JAMAIS sur le repo (public + données personnelles).
Info DB/
# Outillage de migration local (scripts Python + données VIN/factures clients) — hors repo (PII).
# Exception : l'extension navigateur My Ducati, qui est du code applicatif versionné.
tools/*
!tools/myducati-extension/
# Exception : chargeur du catalogue Ducati (code seul ; les fichiers de données restent dans Téléchargements).
!tools/catalog-loader/
# Exception : chargeur des plans d'entretien (code seul ; les données restent dans entretiens-extraits).
!tools/maintenance-loader/
# Exception : générateur de la table des motifs de VIN (code seul ; les VIN restent dans Téléchargements).
!tools/vin-patterns/
# Exception : chargeur des catalogues accessoires et vêtements Ducati (code seul ; données dans Téléchargements).
!tools/accessories-loader/
# Exception : données de démonstration des parcours d'entretien (code seul ; les manuels restent
# dans Desktop/ducati/manuels-extraits).
!tools/journey-demo/
# Exception : outils de reprise G8 (code seul ; les exports du client restent hors du dépôt).
!tools/migration/
# Figures des manuels d'atelier copiées pour la démonstration (~300 Mo) — hors repo.
# Les regénérer : node tools/journey-demo/copy-images.mjs
public/manuels/
# Exception : chargeur des manuels d'atelier (code seul ; l'extraction et ses 52 Go d'images
# restent dans Desktop/ducati/manuels-extraits).
!tools/wsm-loader/
# Manuel utilisateur généré (captures pouvant contenir des données client) — hors repo pour l'instant.
docs/manuel/

# Supabase CLI — état local du lien projet (contient l'URL du pooler, etc.)
supabase/.temp/
supabase/.branches/
